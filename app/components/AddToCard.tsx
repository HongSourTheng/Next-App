"use client";
const AddToCard = () => {
  return (
    <div>
      <button onClick={() => console.log("New Product")} className="btn btn-primary">
        Add to Card
      </button>
    </div>
  );
};

export default AddToCard;
